# Build artifacts (moved out by the script, but just in case)
chroot/
binary/
cache/
.build/

# Generated configs (regenerated every build)
config/package-lists/
config/includes.chroot/
config/hooks/
config/binary
config/bootstrap
config/chroot
config/common
config/source

# ISOs, logs and temporary build files
*.iso
*.iso.*
*.log
*.squashfs

# Output directory where ISOs are saved
FurryOS-Builds/

# Common editor and system junk files
.*.swp
*.bak
*.tmp
Thumbs.db
.DS_Store

# Python cache files
__pycache__/
*.pyc
